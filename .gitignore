.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# runtime data — never committed
data/
runs/
private/
*.parquet
*.jsonl
!examples/**/*.jsonl
*.mp4
!examples/**/*.mp4
*.MP4
*.db
*.db-wal
*.db-shm

# secrets
.env
*.key

.DS_Store

# session handoff notes — personal working docs, not project documentation.
# (a prior session's git add -A already swept up one stray working file —
# forced_boxes.json, removed in 8b1b2fa — this is the guard against a repeat.)
handover.md
.mp_models/
